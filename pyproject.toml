[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dp-mujoco-pickplace"
version = "0.0.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["panda_mujoco_gym"]
